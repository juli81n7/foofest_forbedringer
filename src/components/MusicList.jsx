import Song from "./Song";

function MusicList(props) {
  return (
    <section>
      {/*  {props.songs.map((song, index) => (
        <Song name={song.name} key={index} />
      ))} */}
    </section>
  );
}

export default MusicList;
